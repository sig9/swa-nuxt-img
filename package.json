{
  "name": "nuxt-app",
  "private": true,
  "scripts": {
    "build:win": "set NITRO_PRESET=azure&nuxi dev",
    "swa:deploy": "swa deploy",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@azure/static-web-apps-cli": "^1.1.3",
    "@nuxt/devtools": "latest",
    "@nuxt/image": "1.0.0-rc.1",
    "@types/node": "^18",
    "nuxt": "^3.5.2"
  }
}
